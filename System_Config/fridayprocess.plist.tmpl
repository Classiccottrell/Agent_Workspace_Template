<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>__LABEL__</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>__WORKSPACE_ROOT__/System_Config/friday_process.sh</string>
    </array>

    <!-- Friday 16:30. Weekday 5 = Friday. If the Mac is asleep at 16:30, launchd
         runs the missed job once at next wake. RunAtLoad is false: this job should
         only run on Fridays, never just because the agent was (re)loaded. -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Weekday</key>
        <integer>5</integer>
        <key>Hour</key>
        <integer>16</integer>
        <key>Minute</key>
        <integer>30</integer>
    </dict>

    <key>RunAtLoad</key>
    <false/>

    <key>StandardOutPath</key>
    <string>__LOG_DIR__/friday_process.launchd.out</string>
    <key>StandardErrorPath</key>
    <string>__LOG_DIR__/friday_process.launchd.err</string>
</dict>
</plist>
