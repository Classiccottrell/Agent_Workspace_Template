# active/ — work in progress

Placeholder. In a real project, this directory holds the live source you are
actively editing.

For this illustrative example the implementation is intentionally omitted; the
brief and structure are the point. When you copy `_TEMPLATE/` for real work,
put your source files here and move anything superseded into `../archive/`.

Example of what a working note looks like:

- [x] Parse `add` / `list` / `done` subcommands.
- [x] Persist tasks to a plain text file in the home directory.
- [ ] (out of scope per BRIEF) due dates.
