# example — onboarding sandbox

A disposable toy project for trying the orchestrator's delegation flow.

Ask: "Have the coder add a --greet flag to Projects/example/hello.sh" (see
`BRIEF.md` for the full spec).

Deletable — remove this directory anytime, it's not referenced elsewhere
(except WELCOME.md, which is equally disposable).

For a filled-in **reference brief** (what "done" looks like), see
`../example-project/` — that one stays.
